import 'bootstrap-css-only/css/bootstrap.min.css';

function App() {
	return (
		<>
			<div>hienkk</div>
			{/* <BrowserRouter>
				<Routes>
					{routes.map((route, index) =>
						<Route
							key={index}
							path={route.path}
							exact={route.exact}
							element={route.component}
						/>)
					}
				</Routes>
			</BrowserRouter> */}
		</>
	);
}

export default App;
